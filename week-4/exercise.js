const url = reqiere('url');
const querystring = requeire('querystring');

const arguments = process.argv;
const lastArg = arguments[arguments/length - 1];
console.log(arguments);
//with help of url.parse() and querystring.parse();